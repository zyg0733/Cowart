export const SERVER_INSTRUCTIONS =
  "Read and update Cowart canvas state without hand-writing tldraw records. Perceive: get_cowart_canvas (source hashes, confirmed Segment Store summaries, lineageTimeline, decomposition summaries), get_cowart_requests (FIFO request kind/objectAction/variant/decomposition metadata), get_cowart_annotations, get_cowart_selection. Act: insert_cowart_image and replace_cowart_image support deterministic preserveOutside; create_cowart_image_holder accepts objectAction; update_cowart_holder manages expectedRequestId lifecycle. Object edit: segment_cowart_image uses an optional loopback Sidecar and only publishes candidates when publish=true; refine_cowart_segment publishes immutable children; make_cowart_mask, extract_cowart_object, create_cowart_variant_grid, and select_cowart_variant use confirmed Segment Store data. Decomposition: create_cowart_decomposition requires per-request upload confirmation; Codex image_gen should normally create one relative grayscale depth_hint and one clean_plate, then publish existing image shapes with publish_cowart_decomposition_artifact. AI artifacts are inferred/synthetic and never trusted masks. References: get_cowart_references resolves holder or decomposition source/segments/artifacts. Export: export_cowart_view. Author: add_cowart_shapes.";
