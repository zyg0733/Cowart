export const SERVER_INSTRUCTIONS =
  "Read and update Cowart canvas state without hand-writing tldraw records. Perceive: get_cowart_canvas (structured board with page-local source hashes and confirmed Segment Store summaries), get_cowart_requests (current-page holder generation queue), get_cowart_annotations (批注 text + targets, with target/annotation/selection filters), get_cowart_selection (current selection with confirmed segment summaries when the canvas is reachable). Act: insert_cowart_image (place a bitmap with optional expectedSourceAssetHash and meta.cowartObjectEdit provenance), create_cowart_image_holder (make an AI 图片 slot), update_cowart_holder (request/claim/fail/cancel holder lifecycle with expectedRequestId), replace_cowart_image (fill or swap a bitmap in place, with expectedRequestId and optional expectedSourceAssetHash). Export: export_cowart_view (write an image file the agent can attach; filled holders and single images copy assets directly, pages/selections need the canvas open in a browser to render). Author: add_cowart_shapes (create text/geo/note/line/arrow shapes; arrow fromId/toId binds a connector). Object edit: segment_cowart_image returns browser_interaction_required unless a real server provider is configured, refine_cowart_segment publishes immutable morphology child segments, and make_cowart_mask accepts exactly one of region, regionShapeId, or segmentId. References: get_cowart_references resolves a holder's reference/style images into files/base64 to pass as input_image. get_cowart_canvas also returns each shape's suggestedGenSize (a valid gpt-image size matching its box).";
