interface:
  display_name: "Cowart Image Edit"
  short_description: "Generate revised images from selected Cowart annotations"
  default_prompt: "Use $cowart:cowart-image-edit to read my selected Cowart annotations, generate a clean revised image, and place it beside the original while leaving annotations untouched."
