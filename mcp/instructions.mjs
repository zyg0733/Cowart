export const SERVER_INSTRUCTIONS =
  "Read and update Cowart canvas state without hand-writing tldraw records. Perceive: get_cowart_canvas (structured board with source hashes, confirmed Segment Store summaries, and read-only lineageTimeline), get_cowart_requests (FIFO holder queue with request kind/objectAction/variant metadata), get_cowart_annotations, get_cowart_selection. Act: insert_cowart_image and replace_cowart_image accept expectedSourceAssetHash, meta.cowartObjectEdit provenance, and preserveOutside for deterministic mask-zero RGBA protection; create_cowart_image_holder accepts objectAction; update_cowart_holder manages request lifecycle with expectedRequestId. Object edit: segment_cowart_image returns browser_interaction_required unless a real server provider exists; refine_cowart_segment publishes immutable children; make_cowart_mask materializes a region or confirmed segment; extract_cowart_object creates a source-derived transparent PNG; create_cowart_variant_grid creates 1-6 FIFO holders and select_cowart_variant records a filled winner while retaining alternatives. Export: export_cowart_view. Author: add_cowart_shapes. References: get_cowart_references resolves holder reference/style images into files/base64. get_cowart_canvas also returns suggestedGenSize.";
