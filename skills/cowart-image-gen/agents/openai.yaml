interface:
  display_name: "Cowart Image Gen"
  short_description: "Process queued Cowart AI holder requests"
  default_prompt: "Use $cowart:cowart-image-gen to process current-page Cowart AI image requests sequentially, claim each request with expectedRequestId, and fill the matching holder."

policy:
  allow_implicit_invocation: true
