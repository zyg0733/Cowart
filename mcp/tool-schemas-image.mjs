import {
  canvasDirProperty,
  cowartUrlProperty,
  idempotentWriteAnnotations,
  imageInputProperties,
  objectSchema,
  projectDirProperty,
  writeAnnotations,
} from "./tool-schema-common.mjs";

export const insertImageTool = {
  name: "insert_cowart_image",
  title: "Insert Cowart Image",
  description: "Copy a local bitmap into a Cowart page-local assets folder, create a tldraw image asset and shape, place it beside an anchor or clear page area, and save through the Cowart canvas API.",
  inputSchema: objectSchema({
    ...imageInputProperties,
    projectDir: projectDirProperty,
    canvasDir: canvasDirProperty,
    cowartUrl: { type: "string", description: "Running Cowart URL, for example http://127.0.0.1:43218." },
    pageId: { type: "string", description: "Target tldraw page id. Optional when an anchor or view-state page is available." },
    anchorShapeId: { type: "string", description: "Existing shape id to place beside, usually the source image or AI frame." },
    sourceShapeId: { type: "string", description: "Alias for anchorShapeId." },
    fileName: { type: "string", description: "Optional destination filename under the page assets folder." },
    placement: { type: "string", enum: ["right", "left", "below"], description: "Placement direction from the anchor. Ignored when fillAnchor is true." },
    margin: { type: "number", description: "Canvas units between the new image and nearby shapes. Defaults to 40." },
    matchAnchor: { type: "boolean", description: "Use the anchor display size when possible. Defaults to true." },
    fillAnchor: {
      type: "boolean",
      description: "Fill a LEGACY holder instead of placing beside it: for a legacy AI 图片 frame holder the image is added as a child at 0,0 sized to the frame; for a legacy geo holder it overlays the holder's position, size, and rotation. NOTE: the current cowart-ai-image holder is filled with replace_cowart_image instead (fillAnchor on it is rejected). Defaults to false.",
    },
    displayWidth: { type: "number", description: "Displayed shape width in canvas units." },
    displayHeight: { type: "number", description: "Displayed shape height in canvas units." },
    altText: { type: "string", description: "Image shape alt text." },
    lineageOf: { type: "string", description: "Mark this image as a revision of a previous image shape: records lineage metadata and (unless lineageConnector is false) draws a bound connector from that image to this one." },
    prompt: { type: "string", description: "Generation prompt to record in the lineage metadata (with lineageOf)." },
    provider: { type: "string", description: "Edit provider id to record in object-edit provenance when objectEdit is provided." },
    model: { type: "string", description: "Edit model id to record in object-edit provenance when objectEdit is provided." },
    version: { type: "number", description: "Revision number to record in the lineage metadata (with lineageOf)." },
    lineageConnector: { type: "boolean", description: "Draw the dotted connector arrow from the previous version to this one. Defaults to true when lineageOf is set." },
    annotationScreenshot: { type: "string", description: "Source annotation screenshot filename for metadata." },
    expectedSourceAssetHash: { type: "string", description: "Require the source image asset bytes to still match this SHA-256 before writing." },
    expectedSourceSha256: { type: "string", description: "Alias for expectedSourceAssetHash." },
    objectEdit: { type: "object", description: "Object-edit provenance stored on the inserted shape as meta.cowartObjectEdit." },
    shapeMeta: { type: "object", description: "Additional tldraw shape metadata." },
    assetMeta: { type: "object", description: "Additional tldraw asset metadata." },
    dryRun: { type: "boolean", description: "Calculate insertion without copying or saving." },
  }),
  annotations: writeAnnotations,
};

export const replaceImageTool = {
  name: "replace_cowart_image",
  title: "Replace Cowart Image",
  description: "Replace the bitmap of an existing image shape in place (the 替换 flow), keeping its position and size. If given a frame holder, replaces the image inside it. Copies the new bitmap into the page assets folder and updates the asset reference.",
  inputSchema: objectSchema({
    imagePath: { type: "string", description: "Absolute local bitmap path to swap in. Provide this or imageBase64/imageDataUrl." },
    imageBase64: imageInputProperties.imageBase64,
    imageDataUrl: imageInputProperties.imageDataUrl,
    mimeType: imageInputProperties.mimeType,
    targetShapeId: { type: "string", description: "Image shape id to replace, or a frame holder whose image should be replaced. Falls back to the current selection." },
    shapeId: { type: "string", description: "Alias for targetShapeId." },
    projectDir: projectDirProperty,
    canvasDir: canvasDirProperty,
    cowartUrl: cowartUrlProperty,
    fileName: { type: "string", description: "Optional destination filename under the page assets folder." },
    displayWidth: { type: "number", description: "New displayed width. Defaults to the existing shape width." },
    displayHeight: { type: "number", description: "New displayed height. Defaults to the existing shape height." },
    keepOldAsset: { type: "boolean", description: "Keep the previous asset record/file instead of removing an now-unreferenced one. Defaults to false." },
    assetMeta: { type: "object", description: "Additional tldraw asset metadata." },
    genParams: { type: "object", description: "Full generation call (prompt, refs, size, model, seed, …) recorded on the shape (meta.cowartGen) for reproducibility." },
    expectedSourceAssetHash: { type: "string", description: "Require the current target source asset bytes to still match this SHA-256 before replacing." },
    expectedSourceSha256: { type: "string", description: "Alias for expectedSourceAssetHash." },
    objectEdit: { type: "object", description: "Object-edit provenance stored on the updated shape as meta.cowartObjectEdit." },
    expectedRequestId: { type: "string", description: "For cowart-ai-image holders, require the active meta.cowartRequest.id to match before filling." },
    dryRun: { type: "boolean", description: "Calculate the replacement without copying or saving." },
  }),
  annotations: writeAnnotations,
};

export const exportViewTool = {
  name: "export_cowart_view",
  title: "Export Cowart View",
  description: "Export the canvas to an image file the agent can attach or reference. A single image (or a frame holder's image) is copied losslessly from page assets (no browser needed). Pages, selections, or multi-shape regions are rasterized by a connected Cowart browser via editor.toImage, so the canvas must be open for those.",
  inputSchema: objectSchema({
    projectDir: projectDirProperty,
    canvasDir: canvasDirProperty,
    cowartUrl: cowartUrlProperty,
    outputPath: { type: "string", description: "Absolute destination file path. Defaults to <canvasDir>/exports/cowart-export-<timestamp>.<ext>." },
    mode: { type: "string", enum: ["selection", "shapes", "page", "currentPage"], description: "What to export. Defaults to a single targetShapeId, else selection, else the current page." },
    targetShapeId: { type: "string", description: "Export a single shape (image, or a frame holder's image). Falls back to the current selection." },
    shapeId: { type: "string", description: "Alias for targetShapeId." },
    shapeIds: { type: "array", items: { type: "string" }, description: "Shape ids to export when mode is 'shapes'." },
    pageId: { type: "string", description: "Page to export when mode is 'page'." },
    format: { type: "string", enum: ["png", "jpeg", "svg", "webp"], description: "Output format. Omit to copy a single image losslessly; set it to force a browser render." },
    render: { type: "boolean", description: "Force a browser render even for a single image. Defaults to false." },
    scale: { type: "number", description: "Render scale factor (render strategy only)." },
    padding: { type: "number", description: "Render padding in canvas units (render strategy only)." },
    background: { type: "boolean", description: "Include the page background in the render (render strategy only)." },
    returnBase64: { type: "boolean", description: "Also return the exported image as base64 (+mimeType), e.g. to feed a sketch straight into image generation as a reference. Defaults to false." },
    dryRun: { type: "boolean", description: "Resolve the plan (strategy + output path) without writing or rendering." },
  }),
  annotations: idempotentWriteAnnotations,
};
